### [Chapter II: Proposal Details - Technical Input]

[Instructions]
- Provide the following technical details to the AI.
- The AI will use this data to generate the "Ⅱ. Proposal Details" section in Korean.
- Ensure the AI expands the **Sensitivity Analysis** and **Implications** sections with high technical detail.

---

1. Analysis and AI Model Detailed Specifications:
   - Data Preprocessing:
      1. Using kiwipiepy morphological analyzer for precise tokenization of legal proper nouns and statutory numbers.
      2. Fusing metadata (decision title, respondent, violation type) as a Prefix to chunk text to prevent context fragmentation.
   - Chunking Strategy: Adhering to the official Hybrid JSON chunking logic (512 or 1024 tokens) to maintain semantic alignment with ground truth data.
   - Retrieval and Reranking Pipeline:
      1. Hybrid Search implementation combining Dense (BGE-M3) and Sparse (SPLADE/BM25) signals.
      2. Dynamic RRF (Reciprocal Rank Fusion) weighting experiments, specifically prioritizing SPLADE for precise keyword matching in legal clauses.
      3. ColBERT-based re-ranking to refine top-K candidates.
   - System Workflow:
      1. Intent classification via LangGraph StateGraph.
      2. Dynamic routing to specialized retrieval paths.
      3. Hybrid search and mathematical rank fusion.
      4. Answer generation using the selected 8B+ parameter LLM.

2. Interpretation of Results and Implications (Detailed Sensitivity Analysis):
   - Synthetic Data Evaluation Methodology: 
      1. Leveraging Gemini 3 Flash Preview to generate a high-quality Gold Standard (Question, Reference Answer) from the provided 500 decisions.
      2. Measuring Recall@5, MRR, BERTScore, and F1-Score across various model combinations.
   - Implications and Sensitivity Analysis Findings:
      1. Conducted multi-dimensional Sensitivity Analysis by varying chunk sizes, retrieval weights (Alpha), and model variants to identify performance bottlenecks.
      2. Findings indicate that for FTC legal documents, which are characterized by high text density and complex cross-references, a combination of 'BGE-M3 Dense + SPLADE Sparse' with high RRF weighting for SPLADE yields the most stable results.
      3. This systematic identification process ensures that the selected technical stack is not just a general-purpose RAG but is empirically calibrated for the specific linguistic patterns of Fair Trade legal documents.
      4. The analysis provides a robust foundation for selecting the final 8B or larger parameter generative model that maintains the highest factual accuracy (BERTScore) in the synthetic benchmark.

3. Expected Benefits:
   - Enhanced Administrative Efficiency: Maximizing the productivity of FTC investigators through an empirically verified RAG pipeline.
   - Public Reliability: Providing accurate legal summaries for the public by selecting the model that demonstrates the highest precision in sensitivity testing.

[Constraint Checklist for AI]:
- Output must be in Korean (문어체).
- No arrows (->) - use numbered lists for any sequences.
- No parentheses () - use descriptive sentences.
- Keep technical terms in original English (BGE-M3, SPLADE, ColBERT, etc.).
- Use noun phrase endings (명사형 종결) for lists.
